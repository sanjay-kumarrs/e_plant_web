# ==============================================================================
# Plant Backend Environment Variables Configuration Template
# ==============================================================================
# To override settings, copy this file to a new file named '.env' and modify values.
# Node.js (v20.6.0+) can run with native .env support: node --env-file=.env app.js
# ==============================================================================

# MongoDB Connection URI
# ----------------------
# For local development, install MongoDB Community Server and use:
# MONGODB_URI=mongodb://localhost:27017/plantdb
#
# Default Atlas cloud URI:
MONGODB_URI=mongodb+srv://dvishnu640:vishnu@cluster0.ju3eous.mongodb.net/plantdb?retryWrites=true&w=majority&appName=Cluster0

# Port for Express server (defaults to 8080 if not specified in app.js, 
# you can extend app.js to use process.env.PORT)
PORT=8080
